# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev      # Next.js dev server on :3000
npm run build    # Production build (also type-checks)
npm run start    # prisma migrate deploy && next start -- runs pending migrations before serving
npm run lint     # ESLint (flat config, includes TypeScript + React Compiler rules)
```

`postinstall` runs `prisma generate` automatically, so a fresh `npm install` always has a matching client.

## Architecture

Next.js 16 App Router, deployed on Railway (not Vercel) as `app.reyse.co.uk` — the admin dashboard for Reyse.

### Auth

NextAuth v4, Google OAuth, JWT sessions, single hardcoded admin-email allowlist in `src/lib/auth.ts` — no roles table. Route protection is `src/proxy.ts` (Next 16 renamed `middleware.ts` → `proxy.ts`; the old `next-auth/middleware` wrapper isn't a valid proxy export on 16, so it's written directly against `getToken`). The matcher protects everything except `/login`, `/api/auth/*`, `/api/public/*`, and static assets. `src/app/admin/layout.tsx` does a second, server-rendered session check so a protected page never flashes unauthenticated.

### Database — Prisma 7, driver adapters are mandatory

Prisma 7 changed connection config significantly from v6 — **do not** put `url = env("DATABASE_URL")` in the `datasource` block in `schema.prisma`; the CLI rejects it (`P1012`, "no longer supported in schema files"). Instead:
- `prisma.config.ts` carries the connection string for the CLI (`migrate`, `generate`, `studio`) via `datasource: { url: process.env["DATABASE_URL"] }`.
- The generator is `provider = "prisma-client"` (not the old `prisma-client-js`), output to `src/generated/prisma` (gitignored, regenerated on every install). **The import path is `@/generated/prisma/client`, not `@/generated/prisma`** — there's no root `index.ts`, only `client.ts`.
- At runtime, `PrismaClient` requires a driver adapter — there is no more "just works from schema" direct connection. `src/lib/db.ts` constructs `new PrismaPg({ connectionString: process.env.DATABASE_URL })` from `@prisma/adapter-pg` and passes it as `new PrismaClient({ adapter })`. Omitting the adapter fails at the `new PrismaClient()` call site with a TypeScript error (constructor expects 1 required arg), not a runtime surprise.

Migrations are generated offline (this sandbox can't reach Railway's Postgres directly — only HTTPS egress works, not raw TCP): `npx prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script > prisma/migrations/<name>/migration.sql`, committed, then applied automatically on deploy via the `start` script's `prisma migrate deploy`.

### Overview page

`src/app/admin/page.tsx` — the first screen after login. An async server component querying `db` directly (no client-side fetch) for a handful of real counts: new (status=NEW, non-test, non-deleted) enquiries, active live chats, and Instagram posts published in the last 7 days — each stat tile links to its real page. Leads is the one tile still a static "—", since `src/app/admin/leads/page.tsx` is genuinely unbuilt. Replaced a stale 22 July "foundation build, nothing real yet" placeholder that was never updated as Enquiries/Live Chat/Social/Mail/Analytics were actually built — don't let this drift again if a new feature changes what counts as "real" here.

### Enquiries feature

Captures every lead (website contact form, WhatsApp, Instagram, Facebook, manual) into one Postgres-backed pipeline — status tracking (New/Contacted/Won/Lost), notes, soft-delete with restore (`deletedAt`, never a hard delete), and stats (7/30-day volume, avg response time, win rate, top channel).

- `prisma/schema.prisma` — `Enquiry` model, `EnquiryChannel`/`EnquiryStatus` enums, indexed on `status`, `channel`, `deletedAt`, `createdAt`.
- `src/lib/enquiries.ts` — `validateEnquiryInput()`, shared between the public and dashboard creation paths so they can't validate differently.
- `src/app/api/public/enquiries/route.ts` — unauthenticated (no session exists on the caller's side), protected instead by a shared-secret header (`x-api-key` matched against `INTERNAL_API_SECRET`). This is what Reyse-Website's `api/contact.ts` calls server-to-server, independent of that form's Resend email — either path failing still leaves the enquiry captured by the other. Requires `INTERNAL_API_SECRET` to be set to the *same* value on both this app's Railway service and the Reyse-Website Vercel project; the route fails closed (500) if its own copy is unset, and 401s on the wrong or missing key.
- `src/app/api/enquiries/**` — session-protected CRUD (list+filter, manual/test add, PATCH status/notes, soft-delete, restore) for the dashboard itself. List is capped at 200 rows (`take: 200`, no pager UI yet).
- `src/app/api/enquiries/stats/route.ts` — always filters out `isTest` and soft-deleted rows so test/deleted data can't skew real numbers. `last7DaysCount` and 30-day `volumeByDay` bucketing are computed server-side deliberately (not with `Date.now()` in a component during render) — React's purity/`set-state-in-effect` lint rules flag date-dependent calculations done at render time.
- `src/app/admin/enquiries/page.tsx` + `src/components/enquiries/*` — the UI. Data fetching is a plain `useEffect` + `fetch` (no query library in this app yet); `react-hooks/set-state-in-effect` flags this pattern regardless of await timing since it's aimed at nudging toward Suspense/query libraries — there's a scoped `eslint-disable` on that one line with the reasoning inline.

### Env vars this feature needs

- `DATABASE_URL` — Postgres connection string (Railway).
- `NEXTAUTH_SECRET`, Google OAuth client ID/secret — existing auth requirements, unrelated to this feature.
- `INTERNAL_API_SECRET` — shared secret for `/api/public/enquiries`, must match Reyse-Website's Vercel env var of the same name.

### Live Chat (admin side)

The dashboard side of the Claude-powered widget on reyse.co.uk (see Reyse-Website's own CLAUDE.md for the widget/backend itself). Reyse-Website's `api/chat.ts` calls the routes below server-to-server with the shared `INTERNAL_API_SECRET`, same pattern as the enquiries write.

- `prisma/schema.prisma` — `ChatConversation` (`status` ACTIVE/CLOSED, `topic`, `convertedToEnquiry`, soft-delete via `deletedAt`), `ChatMessage` (`role` USER/ASSISTANT/OPERATOR), `ChatKnowledge` (the same table `captionGenerator.ts` reads for social).
- `src/app/api/public/chat/{conversations,knowledge,stream}/**` — the unauthenticated/shared-secret routes the website calls (find-or-create conversation, persist messages, fetch the knowledge base) plus the SSE stream endpoint (no secret — the unguessable conversation id is the access control, matching `/api/public/social/assets/[id]`'s trust model).
- `src/app/api/chat/**` — session-protected CRUD for the dashboard: list+filter conversations, stats, and `PATCH /api/chat/conversations/[id]` for notes and `status`.
- `src/app/admin/live-chat/page.tsx` + `src/components/chat/*` — the UI. `ConversationModal` shows the live conversation over the same public SSE stream the visitor's widget listens on, lets Morgan reply live (posted as an `OPERATOR` message), edit notes, and toggle `status` between Active and Closed. That toggle was added because nothing set a conversation to CLOSED before — every conversation defaulted to ACTIVE and stayed that way forever, which made "active chats" meaningless as a metric (see the Overview page below). `KnowledgeModal` edits the same `ChatKnowledge` content the Social caption generator reads.

### Social media automation

Generates and posts real single/carousel content to Reyse's own Instagram and Facebook accounts (promoting the business itself, not a per-client feature) — captions and hashtags via Claude, images either AI-generated photography (Replicate) or branded template graphics (rendered in-process), reviewed in a queue before anything goes live by default. Built 22 July 2026.

**Why this shape:** the old `reyse` repo had three separate, never-reconciled systems for this (a single-post pipeline, a carousel pipeline, a story pipeline), each with its own image source, its own storage backend, and even a different env var for the same Instagram account — explicitly judged not good enough. This is one unified pipeline instead: one content-generation path, one image-generation path (that internally chooses AI photography or a template per post), one Graph API client, one scheduler.

- `prisma/schema.prisma` — `SocialPost` (one row per platform per post; two rows sharing a `groupId` are a cross-posted Instagram+Facebook pair, generated together with identical content but published/edited independently), `SocialPostImage`, `SocialAsset` (image bytes stored in Postgres, not external blob storage — see below), `SocialSettings` (singleton row, publishing mode + cadence), `AiUsageLog` (generic, reused by any future feature with AI/paid-API costs).
- `src/lib/social/pillars.ts` — the six content pillars (Education, Tips, Promotion, Social proof, Behind the scenes, News) and the rotation order the scheduler advances through. Social proof and Behind the scenes are explicitly instructed never to fabricate a customer name, quote, or stat — Reyse's old website had fake testimonials and they were removed for exactly that reason; this is a standing constraint, not a one-off note.
- `src/lib/social/captionGenerator.ts` — Claude (`claude-opus-4-8`, adaptive thinking, `effort: "high"`, structured JSON output via `output_config.format`) grounded in the **same `ChatKnowledge` table the live chat widget uses** — one shared source of truth for pricing/feature facts across both surfaces, edited from either the Live Chat or Social admin page (same modal, same underlying content). System prompt encodes the caption structure (hook/value/CTA), the no-em-dash / no-AI-cliché brand voice rules, and 3-5 hashtags.
- `src/lib/social/contentValidation.ts` — an automated quality gate (em dash check, banned AI-slop phrases, hashtag count, caption length) run on every generation; a failure triggers one retry with the specific feedback appended before falling through to human review regardless. Not a replacement for the review queue, a first line of defence so obviously-off-brand output doesn't even reach it looking finished.
- `src/lib/social/imageGenerator.ts` + `templateRenderer.tsx` + `fonts.ts` — chooses AI photography (Replicate, `black-forest-labs/flux-1.1-pro`, a consistent camera/lighting style suffix appended to every prompt to avoid "AI slop" and keep the whole feed visually consistent) for Promotion/Social proof/Behind-the-scenes, or a branded template graphic (rendered server-side via `next/og`'s `ImageResponse`, 1080x1350, Reyse's actual brand tokens from Reyse-Website's `index.css`) for Education/Tips (carousels) and News. AI photography silently falls back to a template if Replicate is unavailable or `REPLICATE_API_TOKEN` isn't set — a missing/dead image-gen key degrades the image style, it never blocks a post. **Fonts are bundled via `@fontsource` and read from `node_modules` at render time, not fetched from Google Fonts over the network** — an earlier version fetched fonts.googleapis.com per render and it silently failed in testing (`next/og`'s `ImageResponse` throws "No fonts are loaded" if the font array ends up empty), which would have crashed image generation in production on any network hiccup. Confirmed working via a real rendered test image, not just a type-check.
- `src/lib/social/graphApi.ts` — Meta Graph API client. Instagram: single post is container-then-publish; carousel is child containers (`is_carousel_item`) → parent `CAROUSEL` container → publish, with status polling (`FINISHED`/`ERROR`/`EXPIRED`) at each step, matching the confirmed-working flow from the old repo's carousel code. Facebook: single post is `POST /{page-id}/photos` directly; multi-photo is upload-each-unpublished-then-attach-to-one-feed-post (Facebook has no native organic "carousel" post type). Also implements genuine delete (`DELETE /<id>` on both platforms, confirmed actually supported, not assumed) so "undo" on an already-published post can really remove it live, not just hide the local record.
- `src/app/api/public/social/assets/[id]/route.ts` — public, unauthenticated image-serving route (Meta's servers fetch images by plain HTTPS URL; same trust model as the live-chat SSE stream, an unguessable cuid is the access boundary). Images are stored as `Bytes` directly in Postgres rather than a separate blob store (Azure Blob, S3) — one less external dependency that could be misconfigured with nobody around to fix it, and the old repo's carousel system already used the same "Postgres as blob store" pattern successfully.
- `src/lib/social/scheduler.ts` + `src/instrumentation.ts` — a `setInterval` started from Next's `register()` boot hook, checking every 5 minutes. Confirmed as the old `reyse` backend's actual working mechanism for its Instagram autopilot (read directly from its source), not guessed — its own `CRON_JOBS.md` documented a *different*, unused mechanism, a docs/code drift this rebuild's CLAUDE.md is specifically trying not to repeat. Only makes sense on Railway's persistent Node process, not Vercel's stateless functions. Generates a new cross-posted pair on a cadence derived from `SocialSettings.postsPerWeekInstagram`; publishes anything `SCHEDULED` and due **regardless of publishing mode** — reaching `SCHEDULED` already means approval happened (human, in review-queue mode; automatic, in autonomous mode), so gating the publish step on mode too would mean an approved post in review-queue mode never actually goes out.
- `src/app/admin/social/page.tsx` (nav label "Social") + `src/components/social/*` — review queue UI: filter by status/pillar, a card grid with real image thumbnails (not a text table — reviewing photos and carousels needs to see them), a detail modal with editable caption/hashtags and full status-transition controls (approve & schedule / reject / publish now / undo / delete / restore), a settings panel (publishing mode toggle, cadence), and a manual "Generate now" button that bypasses the cadence timer.
- **Publishing mode is the one safety-critical control** (`SocialSettings.publishingMode`): `REVIEW_QUEUE` (default) means every generated post sits as a draft until a human approves it; `AUTONOMOUS` means the pipeline auto-approves and the scheduler publishes with no human ever looking at it. Defaults to `REVIEW_QUEUE` deliberately — this posts to a live business's real public social presence, and the first batch should be seen before anything goes out unsupervised.
- `src/lib/aiUsageLog.ts` + `/api/analytics/ai-usage` + `src/app/admin/analytics/page.tsx` — generic AI/paid-API cost logging (modelled on the old backend's `AiUsageLog`, minus the mess: no per-tenant field, one shared table for every feature) and a real usage summary replacing the Analytics placeholder. First feature to actually build this — it was flagged as a gap when the live chat widget shipped ("whichever feature needs it first should build it"). Image-generation cost isn't priced (Replicate bills per-second of compute, not per-image, so there's no honest fixed cost to log) — image calls show as counts, not a dollar figure; check the Replicate dashboard for actual spend.
- `src/app/api/public/ai-usage/route.ts` — the gap closed: an unauthenticated (shared-secret, same `x-api-key`/`INTERNAL_API_SECRET` pattern as `/api/public/enquiries`) route that lets Reyse-Website's own `api/chat.ts` log live chat's Claude token usage here too (`feature: "live-chat"`), so chat spend now shows up on the Analytics page alongside social and mail instead of being invisible. Added because that gap sat open after the social feature built the table but nothing on the chat side ever called it.

**Env vars this feature needs:**
- `ANTHROPIC_API_KEY` — same key the live chat widget uses; should already be set (it originated on this Railway service during the 20 July variable migration, before being copied to Reyse-Website's Vercel project) but **not directly re-confirmed present for this feature** — verify after deploy.
- `REPLICATE_API_TOKEN` — optional. Listed in `How Reyse Works.md` as present on this Railway service but "never confirmed as used or unused" before now. If missing or invalid, AI photography silently falls back to template graphics; nothing breaks.
- `INSTAGRAM_ACCESS_TOKEN`, `INSTAGRAM_USER_ID` — required to actually publish to Instagram. **Not confirmed to exist on this Railway service** — genuinely unknown at build time, unlike `ANTHROPIC_API_KEY`/`REPLICATE_API_TOKEN`. Without them, generation and review work fully; any publish attempt fails with a clear reason shown on the post (`FAILED` status), nothing crashes.
- `FACEBOOK_PAGE_ACCESS_TOKEN`, `FACEBOOK_PAGE_ID` — same as above, for Facebook.
- `PUBLIC_BASE_URL` — optional, defaults to the working `reyse-app-production.up.railway.app` Railway URL (same `app.reyse.co.uk`-isn't-attached-yet workaround used everywhere else in this codebase). Used to build the image URLs handed to Meta's Graph API.

### Lead Generation

Finds prospective clients (independent UK holiday-let owners with their own direct-booking website, per `docs/lead-gen.md`'s ideal-customer-profile) via two Google search channels, classifies and enriches each candidate, and verifies the contact details it finds before they're usable. Built 30 July 2026, replacing the old `reyse` backend's version — Morgan's own verdict on that one: "did what I wanted but nowhere near as well." The full weakness-by-weakness comparison lives in `docs/lead-gen.md`; the short version is it had almost no real duplicate protection (URL-string uniqueness only, so http/https/www variants of the same site duplicated), MX-only email "verification" that couldn't catch catch-all domains, and a real bug where its own "excluded from export" promise silently didn't hold. This rebuild fixes all three directly, not just re-implements the same design.

- `prisma/schema.prisma` — `Lead` (the one table; `domain` is `@unique` and *normalized* — no protocol/www/trailing-slash — so it's a real dedup key, not a raw-string one), `LeadCollection` (optional grouping for one search run/area), `LeadSearchUsage` (daily Google API call counters, not a cost figure — see below), enums `LeadClassification`/`LeadSource`/`LeadEnrichmentStatus`/`LeadEmailVerification`/`LeadInstagramVerification` (all real Postgres enums, not free strings, `@@map`'d `ra_lead_*` per this schema's shared-database convention).
- **Dedup is enforced before any paid/metered call, not just at the database write.** `src/app/api/leads/search/route.ts`'s `alreadyKnown()` checks the normalized domain against every lead ever saved — found before, excluded, contacted, doesn't matter — before spending a Claude classification call on it. A lead row is never hard-deleted (only `excluded: true`), specifically so this memory never resets; excluding a lead and re-finding it later both leave the dedup key intact.
- **Two search channels.** `src/lib/leadgen/places.ts` (Places API **(New)**, not the legacy `maps.googleapis.com/maps/api/place/*` endpoints — legacy can no longer be enabled on a new Google Cloud project and is a frozen product, so this was built against the current one from the start. Text Search biased to real lat/lng coordinates from `src/lib/leadgen/searchTerms.ts`'s `UK_REGIONS` list rather than a free-text place name, then a Place Details call per result for its website. Needs a billing-enabled Google Cloud project; `GOOGLE_PLACES_API_KEY` confirmed NOT present on this Railway service, checked 31 July 2026. **The old "$200/month free credit" is gone — Google retired it in March 2025.** Real pricing instead: Text Search bills Pro SKU (5,000 free/month, then $32/1,000), Place Details bills Enterprise SKU (1,000 free/month, then $20/1,000) — both because the fields this needs (`displayName`, `websiteUri`) sit on those tiers. Morgan's explicit requirement is this must never cost money, so `LeadPlacesMonthlyUsage` (`src/lib/leadgen/usage.ts`) hard-caps each call type BELOW the real free allowance — 4,000/5,000 and 800/1,000 — as a safety margin against this app not knowing the Cloud Billing account's actual cycle-reset date. Verified against a real database that the cap is never exceeded.) and `src/lib/leadgen/customSearch.ts` (Google Custom Search JSON API, genuinely free up to 100 queries/day, no billing card, quota tracked in `LeadSearchUsage` via `src/lib/leadgen/usage.ts` so it stops itself before the free tier runs out and starts costing money; `GOOGLE_CUSTOM_SEARCH_KEY`/`GOOGLE_CUSTOM_SEARCH_CX` **do** already exist on this Railway service — carried over from the old backend during the 20 July variable migration and left as "uncertain, not removed" at the time, confirmed working 30 July 2026. Code reads `GOOGLE_CUSTOM_SEARCH_CX`, not `GOOGLE_CSE_ID` — matches the variable name already there instead of asking for a rename). Search results missing an env var return `503` from the route with the exact env var name in the body rather than crashing, same pattern as the missing Instagram/Replicate tokens elsewhere in this app.
- `src/lib/leadgen/classify.ts` — one Claude Haiku (`claude-haiku-4-5-20251001`) call per candidate site, reading its homepage text against `src/lib/leadgen/icp.ts`'s condensed ideal-customer-profile, returning `INDEPENDENT`/`PLATFORM`/`IRRELEVANT` plus a one-line reason. Explicitly instructed to prefer `IRRELEVANT` over `INDEPENDENT` when unsure — "high quality leads only" means false positives cost more than false negatives here. A homepage that can't be fetched at all becomes `ERROR` (unclassified), kept distinct from a genuine `IRRELEVANT` verdict.
- `src/lib/leadgen/enrich.ts` — regex-first extraction (email/phone/Instagram/LinkedIn/Facebook from the homepage, falling back to a same-site "contact" page if fewer than 2 fields found), then a Claude Haiku call only to fill genuine gaps (missing fields, contact name, or the outreach personalisation line) — mirrors the old backend's waterfall but fixes its permanent-failure behaviour: one retry on a transient homepage fetch failure (`src/lib/leadgen/fetchSite.ts`) before a lead is marked `FAILED`, and `src/app/api/leads/enrich/route.ts` runs a small concurrency pool (4 at once) instead of one-at-a-time with a fixed sleep. Cross-lead email/Instagram-handle collisions are dropped (not just hidden) via a `findFirst` check against every other `Lead` row.
- **Email verification is automatic, not a disconnected manual button** (the old tool's was opt-in and never wired into the enrichment pipeline at all). `src/lib/leadgen/verifyEmail.ts`: MX lookup first (`node:dns/promises`), then a best-effort raw SMTP probe (`node:net`, port 25, `RCPT TO` without ever sending `DATA`) that also probes a random nonexistent address at the same domain in the same session (via `RSET` between probes) specifically to catch **catch-all domains** — the old tool's MX-only check couldn't tell "this exact mailbox exists" from "this domain accepts everything." `RISKY` is a real, distinct state for exactly that ambiguity. **Whether Railway allows outbound port 25 was unconfirmed at build time** (many hosts block it to stop spam) — if the SMTP probe can't connect, this honestly falls back to `RISKY` (MX exists, mailbox unconfirmed) rather than claiming `VALID` on a check that never actually ran. Verify after deploy: search a lead with a known-real email and check whether `emailVerification` lands on `VALID` (SMTP worked) or stays `RISKY` (port likely blocked, in which case MX-only is the real ceiling here).
- `src/lib/leadgen/verifyInstagram.ts` — an unauthenticated fetch to the profile URL (no official API for this without app review). Only a genuine `404` counts as `INVALID` and only a genuine `200` counts as `VALID`; a blocked/rate-limited/timed-out request stays `UNVERIFIED` rather than the old tool's "any error = probably fine" logic, which wasn't actually evidence of anything.
- `src/app/admin/leads/page.tsx` + `src/components/leads/*` — search panel (map/web toggle, region+town picker from `UK_REGIONS`, search-term picker from `SEARCH_TERMS`, optional collection), a live SSE progress feed while a search runs, a result list with inline-editable contact chips and verification badges, per-lead re-enrich/exclude actions, an "enrich pending" bulk action, and CSV export. Outreach/sequence tracking (the old tool's DM/email cadence tracker) was deliberately not carried over — Morgan's steer, leads just get a status here, no cadence tooling.
- `src/app/api/leads/export/route.ts` — the one and only read path for CSV export, filters on `excluded: false` directly on the `Lead` row. This is the specific fix for the old tool's real bug: its "blocked contacts" list was a second table that the export code never actually checked, so excluded contacts leaked into CSVs despite the UI promising otherwise. Here there's one flag and one code path, so that drift can't recur.

**Env vars this feature needs:**
- `GOOGLE_PLACES_API_KEY` — Places API (New) (Text Search + Place Details). **Not set on this Railway service** (confirmed 31 July 2026) — map search returns a clear 503 until it's added. Needs a Google Cloud project with billing enabled; real pricing and the hard monthly safety caps that keep this at £0 are covered above.
- `GOOGLE_CUSTOM_SEARCH_KEY` + `GOOGLE_CUSTOM_SEARCH_CX` — Google Custom Search JSON API key and Custom Search Engine ID (the "cx" name is Google's own parameter name, carried over as-is rather than renamed to the more common "CSE_ID" term). **Both already set on this Railway service** — confirmed present 30 July 2026, carried over from the old backend. No billing account needed; genuinely free up to 100 queries/day, enforced by `LeadSearchUsage`.
- `ANTHROPIC_API_KEY` — same key every other AI feature in this app uses, already set.

### Email Assistant

Full access to Morgan's own Gmail (`morgan.king@reyse.co.uk`) — not a per-client feature. Reads the inbox, organises it into folders (Gmail labels) with full autonomy, and will draft replies in a later phase (never send). Requested 30 July 2026, phase 1 (connect + read + organise) built the same day; drafts and a chat panel are deliberately out of scope for phase 1. Extended 31 July 2026, still within phase 1: real move (not just add) between folders, plus a backfill sweep through the whole existing mailbox — Morgan's original ask covered "take mail out of a folder and put it in another" and "everything I already have," not just new arrivals, and the first build only handled the latter.

**Why a dedicated OAuth client:** a second, separate Google OAuth client ("Reyse Mail Assistant", Web application type) lives in the same Google Cloud project (`managing-emails-491718`) as the one `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` use for dashboard sign-in — kept deliberately apart so nothing this feature does can affect login. Audience is set to **Internal** (the Google Workspace org for `reyse.co.uk` was confirmed live, 30 July 2026), which matters for a real reason: an **External + Testing** OAuth app only issues refresh tokens that expire after 7 days, and Gmail's scopes are Google-classified as restricted, meaning fixing that properly would mean a Google verification review. Internal skips both entirely — no expiry, no verification — because it's Workspace-only and single-user by construction.

A prior attempt at this exact feature exists and was checked before building: an OAuth client called "Email Manager" (Desktop type, 29 March 2026) sits in the same Google Cloud project, built for an n8n workflow Morgan tried and abandoned. Not reused — wrong OAuth type for a server-side web app in any case. See `Email Assistant.md` in the vault for the full history.

- `prisma/schema.prisma` — `MailAccount` (singleton row, id `"singleton"`; holds the AES-256-GCM-encrypted Gmail refresh token, not plaintext, plus `backfillStatus`/`backfillPageToken` tracking the mailbox sweep below), `MailActivityLog` (one row per organising action, what the Mail page's activity feed reads from) + `MailActivityAction` enum (`MESSAGE_FILED` for a first-time folder, `MESSAGE_MOVED` for a real change).
- `src/lib/mail/crypto.ts` — `encryptToken`/`decryptToken`, AES-256-GCM keyed by `MAIL_TOKEN_ENCRYPTION_KEY`.
- `src/lib/mail/googleClient.ts` — the dedicated OAuth2 client (`GMAIL_CLIENT_ID`/`GMAIL_CLIENT_SECRET`, distinct from the sign-in ones), consent URL generation, code-for-tokens exchange, and `getAuthorizedGmailClient()` which loads and decrypts the stored refresh token into a ready-to-use `gmail_v1.Gmail` client. Listens for Google's rare refresh-token rotation and persists it if it happens.
- `src/lib/mail/labels.ts` — every actual Gmail-side folder operation (`listFolders`, `createFolder`, `renameFolder`, `deleteFolder`, `refileMessage`), each one writing its own `MailActivityLog` row in the same place the change happens. `isFolderLabel`/`listFolders` treat only Gmail's `type: "user"` labels as folders — system labels (INBOX, SENT, CATEGORY_*, UNREAD, ...) are never created/renamed/deleted/removed. `refileMessage` diffs a message's current folders against where it should be and does a real move (`addLabelIds` + `removeLabelIds` in one call, including removing INBOX so filing actually gets a message out of the primary inbox view rather than just tagging it — confirmed with Morgan this is the intended behaviour) — it's a no-op (no API call, no log entry) if the message is already correctly filed. Deleting a folder only removes the label; the underlying messages are never deleted, so it's always recoverable.
- `src/lib/mail/organizer.ts` — `classifyMessages` (Claude, `claude-opus-4-8`, structured JSON output, same pattern as `captionGenerator.ts`) decides which folder(s) a message belongs in, strongly preferring an existing folder over inventing a new one. Folder-name matching is case-insensitive and trimmed specifically to stop the model creating near-duplicate folders ("Finance" vs "finance") across separate ticks. `organizeMessages` is shared by both the ongoing new-mail job and the backfill sweep below — an `InboxMessage` carries its `currentFolders`, so the same function handles "never filed before" and "already somewhere, might need to move" identically.
- `src/lib/mail/scheduler.ts` + `src/instrumentation.ts` — two jobs on one 5-minute tick (same pattern as the social scheduler, registered alongside it):
  - **New-mail sync** — deliberately conservative, only touches messages with no folder on them yet, so it never fights a manual filing choice Morgan made between ticks. Uses Gmail's incremental History API once a `historyId` cursor exists; falls back to a bounded fresh inbox scan both on first run and if the cursor has expired (Gmail only retains ~1 week of history). The history sync only listens for `messageAdded` events, not label changes, so `refileMessage`'s own `labelsAdded`/`labelsRemoved` never gets picked back up as a new message to reclassify.
  - **Backfill sweep** — Morgan's 31 July ask: reconsider the *entire* existing mailbox, not just new arrivals, and genuinely move things rather than only add. Pages through everything except Sent/Drafts/Trash/Spam/Chat (`backfillStatus`/`backfillPageToken` on `MailAccount` track progress across ticks), reclassifying every message regardless of its current folder. Starts automatically the moment the account connects (`backfillStatus` defaults to `NOT_STARTED`, which the tick treats the same as `IN_PROGRESS`). Same stale-cursor problem as History API applies to `pageToken` over a sweep spanning hours — same fix: falls back to restarting from the first page rather than getting stuck, safe because `refileMessage` no-ops on anything already correctly filed, so a restart just re-walks already-done mail rather than redoing or undoing real work.
  - Both jobs cap at 20 messages per tick so a big backlog catches up gradually instead of firing one huge AI call. For a large mailbox, expect the backfill to take many hours (or longer) to reach `DONE` — this is deliberate cost/rate-limit control, not a bug; the Mail page shows "Reorganising existing mail..." while `backfillStatus` is `IN_PROGRESS`.
- `src/app/api/mail/{connect,callback,status,labels,messages,activity}/route.ts` — session-protected (belt-and-braces, `proxy.ts` already covers `/api/mail/*`). `connect` starts the consent flow; `callback` is where Google redirects back to, exchanges the code, and redirects into `/admin/mail` with a `connected` or `connectError` query param. `status` includes `backfillStatus` for the UI indicator.
- `src/app/admin/mail/page.tsx` — connect card when no `MailAccount` exists yet; once connected, a three-pane layout (folders, message list, activity feed) plus the backfill-in-progress indicator.

**What's deliberately not built yet:** drafting replies, and any chat/talk panel for asking about the inbox — both explicitly deferred by Morgan on 30 July 2026 pending phase 1 proving out first. Note also that **Talk to Rey already exists** (`src/app/admin/talk`, hosted voice conversation via the Agent SDK pointed at this repo's cloned `vault/`) — when a chat panel for Mail does get built, extending Talk to Rey with a typed mode is the leading option over building a second, separate assistant interface, per the vault's "one Rey, not several" rule. Not decided yet.

**Env vars this feature needs:**
- `GMAIL_CLIENT_ID`, `GMAIL_CLIENT_SECRET` — the dedicated "Reyse Mail Assistant" OAuth client's credentials, from the same Google Cloud project as `GOOGLE_CLIENT_ID` above but a different client entry. Read fresh from `process.env` on every call, not cached at module load (a Railway variable saved right around a container's boot is a real staleness risk otherwise) and `.trim()`-ed (a stray trailing space or newline from a browser copy-paste is invisible in Railway's UI but breaks Google's exact-string client_id match). The connect route fails closed with a plain-language error if either is missing, rather than crashing.
- `MAIL_TOKEN_ENCRYPTION_KEY` — 32-byte key, base64-encoded (`openssl rand -base64 32`), for encrypting the stored refresh token at rest.

**Redirect URI is derived from the actual request, not `PUBLIC_BASE_URL`.** `getConsentUrl`/`connectAccount` take a `baseUrl` param built from `request.nextUrl.origin` in the connect/callback routes, not from an env var. A static `PUBLIC_BASE_URL` (as Social's Meta image URLs still use) drifts the moment the app is reachable at more than one hostname — which it now is, `reyse-app-production.up.railway.app` and `app.reyse.co.uk` both resolve to the same service — and Google requires the redirect_uri at authorization time to exactly match the one used at token-exchange time. Deriving both from whichever domain actually served the request keeps them correct by construction for any domain, with nothing to misconfigure. Practical implication: **every domain the app is actually reached through needs its own `.../api/mail/callback` entry in the "Reyse Mail Assistant" OAuth client's Authorized redirect URIs in Google Cloud Console** — as of this fix, that means both the Railway URL and `https://app.reyse.co.uk/api/mail/callback`.

### DM Automation

Instagram/Facebook DM auto-replies for Reyse's actual paying clients (independent holiday-let hosts) — **not** Reyse itself, unlike every feature above this one. Sold on the pricing page as "Social Media Automation" (£99/month, £1,000 setup) alongside Live Chat and WhatsApp Automation, but had zero backend until this feature. Built as reyse-app's **first genuinely multi-tenant feature**: every model above this section is single-tenant, scoped only to Reyse's own accounts/inbox. Morgan manages every client himself through this dashboard — there is no client-facing login anywhere in this feature, by deliberate choice (see "Client access" decision below).

**Why this isn't a repeat of the last attempt.** A prior Instagram DM bot existed in the abandoned `reyse` repo (single-tenant, hardcoded to Morgan's own account, no per-client onboarding ever built). Reading that repo's actual code (not just secondhand notes) surfaced its real, previously-undiagnosed failure: the webhook's POST signature check used the GET verify-token instead of the actual app-secret HMAC key, so every inbound DM silently 401'd for a real stretch of time with no alert — discovered only by manually DMing the account. This build's `src/lib/dm/webhookAuth.ts` keeps those two secrets under unmistakably different names (`META_WEBHOOK_VERIFY_TOKEN` vs `META_APP_SECRET`), and `src/lib/dm/scheduler.ts` runs an hourly synthetic webhook check against this app's own live public URL specifically so that class of bug can't ship silently again.

**Confirmed product decisions (Morgan, 4 August 2026):** full multi-tenant architecture from day one; booking = live calendar-availability check + auto-confirm; autonomous AI replies with strict, code-level auto-escalation rather than human-approves-every-message; Instagram first, Facebook Messenger as an explicit later phase; Morgan manages every client through reyse-app, no client portal; calendar source supports both iCal feed URL and Google Calendar OAuth, with a confirmed Google-sourced booking pushed onto the client's actual calendar (not read-only).

**Status, 5 August 2026:** shipped 4 August as Phase 1 (Instagram-only core, no calendar), rated an honest 6/10 against the "flawless" brief, then Morgan said to spend the rest of the session's budget making it as good as possible. Everything below reflects that full increment — Phase 2 (calendar/booking) and Phase 4 (Facebook Messenger) from the original design are now both built, plus five real Phase 1 bugs found and fixed, multi-property disambiguation, and a hardened escalation guard. **Still not done:** Meta App Review hasn't been submitted, and nothing here has touched a real Instagram/Facebook/Google account yet — both are genuinely blocked on Morgan's own setup steps outside this codebase, not on anything left to build.

- `prisma/schema.prisma` — the tenant model is named plainly (`Client`, `Property`, `PropertyKnowledgeBase`, `CalendarConnection`, `CalendarBlock`, `Booking`), not `Dm`-prefixed, since a future WhatsApp Automation feature is expected to reuse it rather than invent its own. Everything specific to DM mechanics is `Dm`-prefixed (`DmConversation`, `DmMessage`, `DmActivityLog`, `DmWebhookHealthCheckLog`) since that doesn't generalize to WhatsApp's own webhook shape. `DmMessage.role` is its own enum (`GUEST`/`AI`/`OPERATOR`), deliberately not a reuse of `ChatRole` above — sharing an enum between Reyse's own marketing widget and per-client guest data would be exactly the kind of accidental coupling this schema otherwise avoids, the same principle as why DM conversations don't reuse `Enquiry` either (a client's guest asking about a holiday cottage must never appear on Reyse's own Enquiries page). `AiUsageLog` gained an optional `clientId` for this feature; every other feature's rows stay `null`. `Booking`'s field names deliberately avoid any language implying a write to Airbnb/Booking.com — no third-party API for that exists; it blocks dates in Reyse's own record (and, for a Google-sourced property, on the client's real calendar via `Booking.googleEventId`) and notifies the host. `DmConversation.propertyDisambiguationAttempts` and `Booking.googleEventId` were added as small follow-on migrations after the initial Phase 1 schema.
- `src/lib/dm/crypto.ts` — AES-256-GCM `encryptToken`/`decryptToken`, keyed by its own `META_TOKEN_ENCRYPTION_KEY` rather than reusing `src/lib/mail/crypto.ts` — a leak or rotation of one shouldn't touch the other, and a self-contained ~20-line helper per feature is this codebase's existing pattern (see Mail Assistant above). Encrypts each client's Meta access token and each connected property's Google Calendar refresh token.
- `src/lib/requestUrl.ts` — `getRequestBaseUrl()`, factored out of `src/lib/mail/googleClient.ts` once this feature's Meta OAuth flow needed the exact same "build a redirect URI from the real incoming request, not a static `PUBLIC_BASE_URL`" logic. Generic request-parsing with nothing feature-specific to keep separate, so it's shared rather than duplicated a second time.
- `src/lib/dm/oauthState.ts` — `signOAuthState`/`verifyOAuthState`, signing an arbitrary id (a `clientId` for Meta, a `propertyId` for Google Calendar) with `NEXTAUTH_SECRET`, no scratch DB table needed for a value that only lives a few minutes. Factored out of `metaOAuth.ts` once Google Calendar's own OAuth flow needed the identical CSRF-state logic for a different kind of id; `metaOAuth.ts` now just re-exports thin, named wrappers so call sites still read `signOAuthState(clientId)` rather than a generic `signOAuthState(id)` with no context.
- `src/lib/dm/metaOAuth.ts` — Meta OAuth (Facebook Login for Business, Page-linked flow — chosen over the newer "Instagram API with Instagram Login" path since Facebook Messenger needs a Page-linked identity regardless). `buildAuthorizationUrl`/`exchangeCodeForLongLivedUserToken`/`listMetaPageCandidates` are parameterized by token or client id throughout, never a single shared env var — this is the actual multi-tenant-ification the old repo never attempted. `debugToken()` is the same Graph API diagnostic the old repo's manual panel used, now driven by a schedule instead of a button. `listMetaPageCandidates` (renamed from `listInstagramCandidates`, 5 August 2026) returns every Facebook Page Morgan administers, not just ones with a linked Instagram account — a Messenger-only Page used to be silently hidden from the connect flow entirely; Morgan confirmed that was wrong and asked for it to work. `MetaPageCandidate.instagramAccountId`/`instagramUsername` are optional accordingly; `storeInstagramConnection` is only called when a candidate actually has one, `storeFacebookConnection` always runs. The picker screen (`meta-pages/page.tsx`) keys candidates by `pageId`, not `instagramAccountId`, since that's the one identifier every candidate is guaranteed to have.
- `src/app/api/clients/[clientId]/meta/connect/route.ts` + `src/app/api/dm/meta/callback/route.ts` — session-protected connect/callback. The callback path is fixed (Meta's redirect_uri is per-app, not per-client) with `clientId` recovered from the signed `state` param. Connecting stores **both** an `INSTAGRAM` and a `FACEBOOK` `ClientMetaConnection` row from the same Page-linked OAuth grant (`storeInstagramConnection`/`storeFacebookConnection`, both exported from the callback route) — one connect flow covers both platforms, since Facebook Messenger's send token is the same Page access token Instagram DMs already use. A client with more than one Facebook Page linked to an Instagram account gets sent to `src/app/admin/clients/[clientId]/meta-pages/page.tsx` (candidates stashed in a short-lived encrypted httpOnly cookie, not a new scratch table) to pick the right one, finalized by `src/app/api/clients/[clientId]/meta-pages/route.ts` (which also stores both platform rows).
- `src/lib/dm/metaTokenHealth.ts` — scheduled `/debug_token` check per connection (~every 12h), flips `ClientMetaConnection.status` to `NEEDS_REAUTH` on failure/near-expiry so a broken token is caught before a guest's DM silently goes unanswered, never only discovered on-demand.
- `src/app/api/public/dm/webhook/instagram/route.ts` + `src/app/api/public/dm/webhook/facebook/route.ts` + `src/lib/dm/webhookIngest.ts` + `src/lib/dm/webhookAuth.ts` + `src/lib/dm/syntheticHealthCheck.ts` — both routes already excluded from `src/proxy.ts`'s session-check matcher (`api/public/*` is carved out there, no change needed). GET handles Meta's one-time subscription challenge; POST verifies `X-Hub-Signature-256` (HMAC-SHA256 over the *raw* body, read via `request.text()` before any JSON parsing, compared with `crypto.timingSafeEqual`) and durably ingests the message **before** responding 200 — closing the old repo's real "fire-and-forget after ack, lost on crash" gap at the receipt step. The two routes are near-identical (`object` is `"page"` not `"instagram"`, `entry[].id` is a Facebook Page id rather than an Instagram-scoped account id) and share the actual ingestion logic via `webhookIngest.ts`'s `ingestMessagingEvent(platform, entryId, event)` rather than maintaining two copies of delicate idempotency/echo handling that could silently drift apart. Idempotent via a unique constraint + upsert on Meta's own message id (`DmMessage.externalMessageId`), the same fix the old repo's own bug-fix commit landed for the echo-arrives-before-write race — confirmed for real against local Postgres for both platforms (see Testing below), not just reasoned through. A genuinely new echo (Morgan replying manually from his own phone, not through the dashboard or the AI) flips the conversation to `HUMAN_ACTIVE` so the AI doesn't talk over him next turn — a real gap the old repo's own code review flagged and never fixed. Slower work (generating and sending a reply) runs via Next's `after()` hook (same pattern as `src/lib/talk/tools.ts`) so Meta's request isn't held open.
- `src/lib/dm/replyEngine.ts` + `src/lib/dm/escalationGuard.ts` + `src/lib/dm/graphApi.ts` — the AI reply engine, `claude-opus-4-8` (same model as every other AI feature in this app — a DM reply represents a paying client's business to a real guest, at least as high-stakes as a social caption; Instagram/Messenger DMs have no sub-second latency expectation the way live chat does). Grounded in `PropertyKnowledgeBase.content`; if a property has no knowledge base yet, every message is honestly escalated rather than guessed at. Two independent escalation layers, not a self-reported AI confidence score (an LLM's verbalized confidence is a famously unreliable number to hang a safety mechanism on): a deterministic pre-filter (`escalationGuard.ts`, same "hard rules only" spirit as `contentValidation.ts`) on the inbound message before any Claude call, plus an `escalate_to_human` tool available every turn that survives the pre-filter. Hard, code-level rules (not just prompt instructions): an AI-authored reply is never sent outside Meta's 24-hour messaging window regardless of what the prompt says, and never sent containing a shortened/tracking URL (`graphApi.ts`'s `containsShortenedUrl`) — a real Instagram/Facebook ban-risk trigger. A reply that fails validation (em dash, shortened URL, empty) retries once with feedback, then escalates rather than sending bad content, mirroring `contentValidation.ts`'s retry-once pattern. Processing is serialized per conversation via a promise-chain mutex (`processInboundMessage`), since the webhook can legitimately trigger it more than once in quick succession for the same conversation.
  - `runReplyLoop` is a genuine multi-round tool-use loop (bounded by `MAX_TOOL_ROUNDS`), not a single-tool-then-stop shortcut: it collects every `tool_use` block per round, dispatches each through a caller-supplied `executors` map, and feeds `tool_result`s back to continue — `escalate_to_human` stays terminal, everything else continues the loop. This is what let `CHECK_AVAILABILITY_TOOL`/`CREATE_BOOKING_TOOL` (below) get added without touching the loop's mechanics.
  - **Multi-property disambiguation** (`resolvePropertyOrAsk` in `replyEngine.ts`) closes what was a total, permanent gap in the first Phase 1 build: a client with more than one active property used to leave every conversation unbound forever. Now: zero properties escalates honestly (nothing to fix here); exactly one auto-binds silently; more than one sends a short question listing the properties, resolves the guest's free-text answer against `Property.name` (`matchProperty`, exported and unit-tested), and escalates rather than guessing after one ambiguous/unmatched retry (`propertyMatch.test.ts`).
  - **Booking tools** (`CHECK_AVAILABILITY_TOOL`/`CREATE_BOOKING_TOOL`) are only ever offered when the resolved property has an `ACTIVE` `CalendarConnection` — otherwise `CalendarBlock` is empty and every date would look falsely free, so a property with no connected calendar keeps escalating booking questions honestly, same as Phase 1. Tool input schemas carry only guest-supplied facts (dates, name, contact) — never `propertyId`/`clientId`/`conversationId`, which the executor closures capture directly from already-resolved local variables in `processInboundMessageLocked` — so the model is structurally unable to supply which property or client it's touching. `parseGuestDateRange` (exported, unit-tested in `replyEngine.bookingTools.test.ts`) rejects a malformed date or an inverted/zero-night range before it ever reaches `createBooking`.
- `src/lib/dm/calendar/` — Phase 2, calendar/booking:
  - `dates.ts` — `toCalendarDate` (the one shared "UTC-midnight-of-a-bare-calendar-date" conversion used by `ical.ts`, `google.ts`, and the booking tools alike — deliberately rejects any string carrying a real time component rather than silently misinterpreting it) and `rangesOverlap` (a pure reimplementation of the overlap condition `availability.ts` runs as a Prisma/SQL filter, kept as the unit-testable proxy for those exact boundary semantics — checkout day == another booking's check-in day is correctly *not* overlapping). Both covered by `dates.test.ts`, including the checkout/check-in boundary case.
  - `blockSync.ts` — `syncCalendarBlocks(propertyId, source, events)`, the shared upsert-changed/create-new/delete-missing diff logic both `ical.ts` and `google.ts` funnel through, so there's one diff implementation instead of two.
  - `ical.ts` — `fetchIcalText`/`parseIcalEvents` (pure, `node-ical`-based, unit-tested against a hand-built realistic fixture in `__fixtures__/sample-airbnb-export.ics`) and `syncIcalConnection` (fetch → parse → diff → update `CalendarConnection` status). A fetch/parse failure updates `status`/`lastSyncError`/`lastSyncedAt` (the last-*attempt* time, not last-success, so the scheduler's hourly gate doesn't hot-retry a dead feed every 5 minutes) without ever touching existing `CalendarBlock` rows — a transient failure must never un-block real dates.
  - `google.ts` — a **third, dedicated** Google OAuth client (distinct from dashboard sign-in and Mail Assistant's), mirroring `src/lib/mail/googleClient.ts`'s proven shape (fresh-per-call `.trim()`'d env reads, request-derived redirect URI, refresh-token-rotation handling). `normalizeGoogleEvents` is the pure, unit-tested (`google.test.ts`) piece: filters out `status: "cancelled"` events (Google represents a cancellation as an item still present in a full list response, unlike iCal where a removed event is simply absent — without this filter a cancelled Google booking would never get removed by `syncCalendarBlocks`'s "delete what's missing" diff) and non-date-only (timed) events. `pushBookingToGoogleCalendar`/`removeBookingFromGoogleCalendar` write/remove the real event on a confirmed booking; both are best-effort from the caller's side (a Google failure never undoes an already-confirmed booking in Reyse's own record). **A dedicated External-audience OAuth client requesting the write scope (`calendar.events`) for other people's Google accounts *will* trigger Google's own verification review the moment anyone besides Morgan's own registered test account uses it** — the admin UI's connect option is labelled "Testing only until Google review completes" for exactly this reason, not just noted in a code comment.
  - `availability.ts` — `isRangeFree` (queries `CalendarBlock` only, never `CalendarConnection.source` — the entire point of normalizing iCal/Google/Reyse's-own-bookings into one shared shape) and `createBooking` (auto-confirm, no human review step — Morgan's explicit decision; re-checks availability inside the same transaction that creates the `Booking` + `CalendarBlock(REYSE_BOOKING)` row, closing the race where two concurrent guest conversations both pass an earlier check; best-effort Google push; writes `DmActivityLog(BOOKING_CONFIRMED)`; calls `notifyBooking`). `cancelBooking` is the admin-UI-only counterpart (never called from the AI reply path — a cancellation needs a human's judgement, same as any other "commitment beyond what the knowledge states" case) — cancels the `Booking`, deletes its `CalendarBlock`, best-effort removes the real Google event, and logs `DmActivityLog(BOOKING_CANCELLED)`.
  - `src/app/api/clients/[clientId]/properties/[propertyId]/calendar/{route,sync}.ts` + `.../calendar/google/connect/route.ts` + `src/app/api/dm/calendar/google/callback/route.ts` — session-protected connect (iCal URL paste, or Google's own OAuth redirect)/disconnect/manual-sync routes, plus the fixed Google callback path (state carries the `propertyId`, same signed-state mechanism Meta's OAuth flow uses via the now-shared `oauthState.ts`). Connecting either source triggers an immediate best-effort sync rather than waiting for the next scheduled tick.
  - `src/app/api/clients/[clientId]/bookings/{route,[bookingId]/route}.ts` — session-protected list (all of a client's bookings across every property) and cancel action.
  - Calendar sync also runs as a third scheduler job (below), hourly, on top of the manual "Sync now" admin action.
- `src/lib/dm/dmStream.ts` — in-memory pub/sub for the live conversation view, a direct copy of `src/lib/chatStream.ts`'s pattern (same "only works on Railway's one persistent process" caveat). `src/app/api/dm/conversations/[id]/stream/route.ts` is its SSE endpoint — deliberately **not** under `api/public` and **not** unauthenticated like `/api/public/chat/stream`: Live Chat's stream has to be public because an anonymous site visitor's own browser connects directly, cross-origin, with no session. Nothing here has an equivalent anonymous party — only Morgan, already signed in, from this same app, ever opens a DM conversation's live view — so this is a normal session-protected route instead of copying a pattern built to solve a problem this feature doesn't have.
- `src/app/api/clients/[clientId]/conversations/[id]/reply/route.ts` — Morgan's live reply, flips the conversation to `HUMAN_ACTIVE` and logs `TAKEN_OVER_BY_OPERATOR`. Inside Meta's 24h window it's a normal send; outside it, the only way to reach the guest at all is the `HUMAN_AGENT` message tag, which Meta's policy restricts to genuine human replies for support purposes — this route is the **only** caller anywhere in this codebase allowed to pass it; `replyEngine.ts`'s AI-authored sends never do. `src/app/api/clients/[clientId]/conversations/[id]/route.ts`'s `PATCH` handles the other half of the takeover lifecycle (`action: "return_to_ai"`) and the per-conversation `aiEnabled` kill-switch toggle (`action: "set_ai_enabled"`).
- `src/lib/dm/graphApi.ts` — `sendInstagramMessage` and `sendFacebookMessage` (near-identical Graph API send calls, different host — `graph.instagram.com` vs `graph.facebook.com`) behind one dispatcher, `sendPlatformMessage({ platform, accountId, ... })`, which is the only thing `replyEngine.ts` and the operator reply route actually call — neither has to branch on platform itself. `accountId` is the Instagram-scoped account id for `INSTAGRAM` or the Facebook Page id for `FACEBOOK`, both already sitting in `ClientMetaConnection.externalAccountId` regardless of platform.
- `src/lib/dm/notifications.ts` — reyse-app's **first own use of Resend**; every other email in this business (contact form, Live Chat's `send_enquiry`) is sent from the separate Reyse-Website Vercel project, but DM Automation's notifications have to originate here since this app is the one holding the live Meta webhook connection. `notifyEscalation` and `notifyBooking` email a client's own `notificationEmail`; `notifyPlatformIssue` (used by the scheduler's webhook health check, which isn't scoped to any one client) emails `ADMIN_EMAIL` (now exported from `src/lib/auth.ts`) directly.
- `src/lib/dm/scheduler.ts` + `src/instrumentation.ts` — same 5-minute-tick, `setInterval`-from-`register()` shape as the Social and Mail schedulers (the only scheduling mechanism this stack has — Railway's one persistent Node process, no external cron/queue infrastructure). Three jobs: the Meta token health check, the synthetic webhook health check (throttled to alert at most once per ~6h while a failure persists), and the calendar sync job (hourly, gated on each `CalendarConnection.lastSyncedAt`). Same "bare `setInterval`, no distributed lock, correct only for exactly one process" caveat as the other two schedulers — stated plainly, not silently assumed safe at any scale.
- `src/app/admin/clients/**` + `src/components/clients/*` — the admin UI: a Clients list (`/admin/clients`, nav entry distinct from "Leads" — Clients means onboarded paying businesses, Leads means prospects) and a tabbed per-client detail page (Properties incl. knowledge-base editor and a per-property Calendar modal — iCal paste, Google connect with its "testing only" label, sync status, manual sync, disconnect; Meta connection status with a Reconnect action once a connection leaves `ACTIVE`; Conversations with a live-takeover modal and a per-conversation AI on/off toggle; Bookings, a flat list across every property with cancel; AI usage cost via `/api/analytics/ai-usage?clientId=`).

**What's deliberately not built yet:** any real client onboarding (Morgan's own account is client zero, not yet connected — blocked on his Instagram being linked to a Facebook Page he administers), and Meta App Review submission (see below) plus its separate, second Facebook Messenger review (`pages_messaging` needs its own submission, not covered by an Instagram-only review).

**Testing:** Vitest (`npm test`, config at `vitest.config.mts`) now covers, beyond the original Phase 1 `webhookAuth.test.ts`/`escalationGuard.test.ts`: `propertyMatch.test.ts` (disambiguation matching), `runReplyLoop.test.ts` (the multi-round tool-use loop against a mocked Anthropic client — text-only, terminal `escalate_to_human`, multi-round tool execution, round-budget exhaustion), `replyEngine.bookingTools.test.ts` (date-range validation), `dates.test.ts` (the shared calendar-date conversion and overlap semantics, including the checkout/check-in boundary), `ical.test.ts` and `google.test.ts` (the pure parse/diff logic for each calendar source). `escalationGuard.test.ts` was extended alongside a real hardening pass over the trigger list itself — see "Escalation guard" below.
  - **Idempotent message ingestion and cross-tenant isolation — closed for real this time, for both platforms.** Phase 1's own CLAUDE.md flagged this as untested ("needs a real or mocked Postgres... wasn't attempted here"). With local Postgres proven reachable in this sandbox, both were verified directly: two clients, two fake Page ids, synthetic HMAC-signed payloads POSTed to each webhook route, confirmed via direct Postgres queries that (a) each message routed to the correct client with no cross-contamination, and (b) redelivering an identical payload produced exactly one `DmMessage` row, not two.
  - The full Phase 2 calendar pipeline was verified the same way, end to end against real local Postgres and a real running dev server: connect an iCal feed (a local static file server serving `__fixtures__/sample-airbnb-export.ics`) → sync → confirm the right `CalendarBlock` rows exist → edit the fixture and re-sync → confirm the diff (removed event's block deleted, new one created, untouched rows' `updatedAt` unchanged) → point the URL at a 404 → confirm `status`/`lastSyncError` reflect it *without* deleting existing blocks → call `isRangeFree`/`createBooking` directly against the real synced data (a known-booked range, a known-free range, and the checkout=check-in adjacent-but-not-overlapping case) → book a free range, confirm a conflicting booking on the same range correctly fails → cancel it via the admin UI, confirm the `Booking` status flips, its `CalendarBlock` is deleted, and both `BOOKING_CONFIRMED`/`BOOKING_CANCELLED` land in `DmActivityLog`. The admin UI itself (connect, status display, Sync now, Bookings tab, Cancel) was driven with a real headless browser against a self-minted NextAuth session, same technique Phase 1 used.
  - Google Calendar's actual OAuth flow, token exchange, and live `calendar.events.insert` call remain genuinely unverifiable without real Google credentials — stated plainly rather than glossed over; what's verified instead is the pure event-diff logic (`google.test.ts`, including the `status: "cancelled"` case) and a careful line-by-line comparison against `googleClient.ts`'s already-proven pattern.
  - Real end-to-end verification of anything requiring live Meta/Google credentials remains Meta's 25-test-user sandbox and Google's own OAuth flow — DM the connected account from a second real Instagram/Facebook account and confirm a reply arrives, an escalation-triggering message hands off correctly, and the scheduled synthetic webhook check logs success.

**Escalation guard** (`src/lib/dm/escalationGuard.ts`) was hardened past its Phase 1 first draft with categories a live UK holiday-let guest product genuinely needs: a dedicated, highest-checked-first crisis/self-harm trigger (**the exact holding-reply wording for this category is deliberately still the same neutral generic one every other trigger uses — the tailored copy was flagged as needing Morgan's direct sign-off before it ships, a duty-of-care call, not a copywriting one, and was not freelanced**); more human-request phrasings ("someone from your team", "is there someone I can call", "can a human help", not wanting to talk to "a bot/robot") added narrowly enough not to sweep in "great team!" or "should I call ahead"; British-specific profanity (`wanker`/`twat`/`bollocks`/`pissed off`, deliberately excluding "bloody"/"bugger" as too common in ordinary speech); and holiday-let commercial risk that was previously entirely uncovered — damage/deposit disputes, noise/neighbour/police involvement, review-threat language, and cancellation requests (increasingly important now booking auto-confirms with no human review). The guard's own file comment states plainly that it only ever inspects the single latest message, never a rolling pattern across a conversation — that's exactly why the second layer (the model's own `escalate_to_human` tool, which sees the full history) exists.

**Meta App Review:** `instagram_manage_messages` and `pages_messaging` (both now actually used, not just anticipated) require Meta App Review (Advanced Access) before this can serve any account beyond up to 25 registered test users — typically 2-4 weeks, needs a working demo, a privacy policy URL, a terms URL, and a use-case screencast. One approval per scope covers every future client connected through the same OAuth flow, no per-client re-review, but Instagram and Messenger are separate submissions. **Not yet submitted as of this build.** Prerequisite, not yet confirmed: Morgan's own Instagram account needs to be a Business/Creator account linked to a Facebook Page he administers before the connect flow can work at all for client zero. Reyse-Website's privacy policy now has a dedicated "Instagram & Facebook Messaging Automation" section (added alongside this build, immediately after the existing "AI Live Chat" section) covering what's collected, why, Anthropic processing, retention, and deletion-on-request — **drafted, not yet reviewed or approved by Morgan; needs his sign-off before it's treated as live/accurate,** same as the crisis-trigger wording above.

**Env vars this feature needs:**
- `META_APP_ID`, `META_APP_SECRET` — the Meta Developer App's credentials. Used for the OAuth token exchange, the `/debug_token` health check, and both webhooks' POST signature HMAC key. **Not yet confirmed to exist on this Railway service or in a Meta Developer account** — genuinely unknown at build time, same category as Social's `INSTAGRAM_ACCESS_TOKEN` was before that feature shipped.
- `META_WEBHOOK_VERIFY_TOKEN` — a value Morgan chooses and enters in Meta's webhook subscription config. Deliberately a different secret from `META_APP_SECRET` — see "Why this isn't a repeat" above for why that distinction is the single most important thing to keep right in this feature.
- `META_TOKEN_ENCRYPTION_KEY` — 32-byte key, base64-encoded (`openssl rand -base64 32`), for `src/lib/dm/crypto.ts`.
- `GOOGLE_CALENDAR_CLIENT_ID`, `GOOGLE_CALENDAR_CLIENT_SECRET` — the third, dedicated Google OAuth client's credentials (see `google.ts` above). Not yet created/confirmed — genuinely unknown at build time.
- `RESEND_API_KEY` — new to reyse-app itself; `resend` was added to `package.json` for this feature.
- Reused, already present: `DATABASE_URL`, `ANTHROPIC_API_KEY`, `PUBLIC_BASE_URL` (the synthetic webhook check's self-POST target and, for Social, its own image URLs).
